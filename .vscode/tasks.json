{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Python File (Workspace Root)",
            "type": "shell",
            "command": "${command:python.pythonPath} ${file}",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}